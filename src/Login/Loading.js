import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center h-screen w-full screenColor'>
            <button className="btn  btn-primary loading">Loading...</button>
        </div>
    );
};

export default Loading;